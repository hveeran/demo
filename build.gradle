plugins {
    id 'com.example.gradle-project-conventions'
    id 'com.example.docker-project-conventions'
    id 'com.example.java-project-conventions'
    id 'com.example.spring-boot-project-conventions'
    alias(libs.plugins.lombok)
    alias(libs.plugins.spring.boot)
}

dependencies {
    implementation(libs.spring.boot.starter)
    implementation(libs.shared.spring.liquibase)
    testImplementation(libs.spring.boot.starter.test)
    testImplementation(libs.bundles.testcontainers)
    testImplementation(libs.mongodb.sync)
    implementation(libs.log4j2.ecs.layout)
}
